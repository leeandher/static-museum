{
  "name": "md-prevuer",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "pnpm": "10.7.1",
    "node": "23.10.0"
  },
  "scripts": {
    "setup-shared": "mkdir -p public/_shared && cp -R ../_shared public",
    "dev": "pnpm run setup-shared && vite",
    "build": "pnpm run setup-shared && vite build"
  },
  "dependencies": {
    "file-saver": "2.0.2",
    "marked": "0.6.2",
    "vue": "3.5.14"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "5.2.0",
    "vite": "6.0.6"
  }
}
